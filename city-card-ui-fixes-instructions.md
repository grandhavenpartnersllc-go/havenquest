# Claude Code Instructions — City Card UI Fixes

## What You're Doing

Two small UI fixes in one file. Read `city-card-ui-fixes-brief.md` completely before touching anything.

---

## Before You Start

1. Read `city-card-ui-fixes-brief.md` completely
2. Read `components/results/CityMatchCard.tsx` in full
3. Confirm back what you are changing before writing any code

---

## One File Only

`components/results/CityMatchCard.tsx` — nothing else.

Do not touch `MarketBadge.tsx`. Do not touch any other file.

---

## When Done

1. `tsc --noEmit` passes clean
2. Both acceptance criteria pass
3. Commit hash

---

*Instructions prepared by Claude (COO) — May 30, 2026*
