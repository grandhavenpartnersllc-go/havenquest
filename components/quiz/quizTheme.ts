// Brand tokens for the Discovery Intake, aligned to the homepage reface that shipped
// today (navy #081426 / gold #C9A961 — mirror of --color-hq-navy / --color-hq-gold in
// globals.css). BLUE is retained only until the per-card CTAs move to gold.
export const NAVY = '#081426'
export const BLUE = '#0076B6'
export const GOLD = '#C9A961'

// Supporting neutrals from the intake visual reskin brief (canonical, not the mockup's
// stale palette). Additive — kept as named exports so the header, tiles and cards can
// consume one source instead of scattering raw hexes.
export const STONE = '#F2F1EE'   // page background
export const CARD = '#ffffff'    // card surfaces
export const INK = '#1c2430'     // body text
export const MUTED = '#6a7180'   // secondary text
export const LINE = '#dcdad2'    // borders, inactive dots
export const RADIUS = '16px'
export const SHADOW = '0 1px 2px rgba(10,30,61,.04), 0 12px 32px rgba(10,30,61,.06)'
